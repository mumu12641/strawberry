// this is a comment.

class Main inherits A{
    b : Int = 0;

    function main(ii:Int) -> Int {
        let s:String = "hello";
        let t:Int = 0;
        t = 1;
        t.print_str(s);
        self.print_int(1 + 1);
        print_int(1 + s);
        return 0;
    };

    function funa(a:Int) -> Int{
        
    };
};

class A {
    a:Int = 0;
    fun funa(a:Int) -> Int{
        let i:Int = 0;
    };
};
